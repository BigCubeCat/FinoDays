import {createTheme} from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#1d2125',
    },
    secondary: {
      main: '#8c376d',
    },
  },
});

export default customTheme;
