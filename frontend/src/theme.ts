import {createTheme} from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#1d2125',
    },
    secondary: {
      main: '#4A486e',
    },
  },
});

export default customTheme;
